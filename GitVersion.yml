mode: ContinuousDeployment
branches:
  main:
    regex: ^main$
    increment: Patch
    prevent-increment-of-merged-branch-version: true
  develop:
    regex: ^develop$
    increment: Minor
ignore:
  sha: []